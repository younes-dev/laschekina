{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vod</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vod.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ vod.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vod_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vod_edit', { 'id': vod.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
